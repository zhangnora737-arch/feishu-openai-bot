{
  "name": "feishu-openai-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:tail": "wrangler tail"
  },
  "dependencies": {
    "@vercel/functions": "^2.2.12"
  },
  "devDependencies": {
    "wrangler": "^4.20.5"
  },
  "engines": {
    "node": ">=18"
  }
}
